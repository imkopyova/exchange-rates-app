import React from "react";
import styles from './search.less';

export const Search = () => {
    return (
        <form>
            <input className="search-input" placeholder="Enter currency" />
        </form>
    )
}